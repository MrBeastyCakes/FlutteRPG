import 'package:flutter/material.dart';

/// Combat HUD — full-screen takeover view shown during combat.
/// Composes BeastCard, TelegraphStrip, RoundTimerArc, PlayerCardMini,
/// StancePad and QuickslotBar.
class CombatHud extends StatelessWidget {
  const CombatHud({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
